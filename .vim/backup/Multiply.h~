#pragma once
#include <stack>
#include <string>

#include "Bigint.hpp"
#include "Command.h"

class Multiply: public mesa::Command{
  public:
    ~Multiply(){}

    bool execute(std::stack <Bigint>& lhs, const std::string& rhs)const;
};


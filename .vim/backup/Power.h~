#include <stack>
#include <string>

#include "Bigint.hpp"
#include "Command.h"

class Power: public mesa::Command{
  public:
    ~Power(){}

    bool execute(std::stack <Bigint>& lhs, const std::string& rhs)const;
};
